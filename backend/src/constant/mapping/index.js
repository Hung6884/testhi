const courseMapping = require('./course');
const trainingCategoryMapping = require('./trainingCategory');
const trainingVehicleMapping = require('./trainingVehicle');
const datDeviceMapping = require('./datDevice');
const administrationMapping = require('./administration');
const dbConnectionMapping = require('./dbConnection');
module.exports = {
  courseMapping,
  trainingCategoryMapping,
  trainingVehicleMapping,
  datDeviceMapping,
  administrationMapping,
  dbConnectionMapping,
};
